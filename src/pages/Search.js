import React from 'react'

const Search = (props) => {
    // console.log(props)
    const keyword = props.match.params.keyword
    console.log(keyword)
    return (
        <div>
        </div>
    )
}

export default Search
